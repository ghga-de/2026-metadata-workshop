pages:
  stage: deploy
  script:
    - mkdir -p .public
    - cp -r * .public/ 2>/dev/null || true
    - mv .public public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH